name: My First Workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v7

      - name: list files
        uses: ls -ltra
      
      - name: Executing shell script
        run: ./ascii-script.sh
